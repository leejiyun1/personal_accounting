spring:
  # JPA 설정
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

# 로깅 설정
logging:
  level:
    root: WARN
    com.personalaccount: INFO