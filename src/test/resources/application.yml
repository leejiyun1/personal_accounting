spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/personal_accounting
    username: jiyun
    password: jiyun016
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: false
    show-sql: false

  flyway:
    enabled: false

  data:
    redis:
      host: localhost
      port: 6379

# Gemini 설정
gemini:
  api-key: ${GEMINI_API_KEY:test-key}
  api-url: https://generativelanguage.googleapis.com/v1/models/gemini-2.5-flash:generateContent
  cache-url: https://generativelanguage.googleapis.com/v1beta/cachedContents
  timeout: 30000
  max-retry: 3

# JWT 설정
jwt:
  secret: test-secret-key-minimum-32-characters-long-for-hs256-algorithm
  access-token-validity: 1800000
  refresh-token-validity: 1800000

# CORS 설정
cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000}
  allowed-methods: GET,POST,PUT,DELETE,OPTIONS,PATCH
  allowed-headers: "*"
  allow-credentials: true
  max-age: 3600